<%= form_for(@soc_book) do |f| %>
  <% if @soc_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@soc_book.errors.count, "error") %> prohibited this soc_book from being saved:</h2>

      <ul>
      <% @soc_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
 
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
